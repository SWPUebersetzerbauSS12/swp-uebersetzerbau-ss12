Minimaler Befehlssatz:

alloca
store
load

br bedingt
br unbedingt

add, sub, mult, udiv, sdiv, urem, srem

and, or, xor

icmp mit 3 operanden


Weitere Annahmen:

Bis zum ersten define wird der Text entfernt. Nur eine main Funktion.
Eine Zeile ist ein Statement. Blöcke sind durch eine Leerzeile getrennt und beginnen mit
ihrem Label (bis auf Block 1) und enden mit einem Sprung oder Return (letzter Befehl).

------------------------------

Für später:

eine main-funktion, nur int (kein float, vector)

shifts später

arrays?
