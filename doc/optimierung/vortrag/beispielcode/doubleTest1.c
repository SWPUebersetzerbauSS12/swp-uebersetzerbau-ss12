#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {

	double a = 0.5;
	double b = 7.7;
	if(a<b) {
		printf("a kleiner: %f\n", a);
	}
	else {
		printf("b kleiner: %f\n", b);
	}
	return 0;

}
